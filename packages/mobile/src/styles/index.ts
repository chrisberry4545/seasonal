import { colors } from './colors';
import { fonts } from './fonts';
import { sizes } from './sizes';
import { shadows } from './shadows';

export const styles = {
  colors,
  fonts,
  shadows,
  sizes
};
